#include "BTree.h"
#include<string>
#include<iostream>

using namespace std;

//构建二叉树(先序遍历)
BTree InitBtree(string s) {//s为先序遍历输入序列
	int lth = s.length();
	while (lth--) {
		BNode* tem = new BNode();
		if()
	}
 }
